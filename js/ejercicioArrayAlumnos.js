/* 
Pedir por pantalla el nombre de 5 alumnos usando bucle while
Guardelos en un arreglo llamado alumnos
Usando el array de alumnos mostrar un mensaje de bienvenida a cada uno
*/

let alumnos = []

while (alumnos.length < 5) {
    let.push = (prompt ('Ingrese el nombre de una alumno'))
}
console.log(alumnos);

for (let i = 0; i < alumnos.length; i++) {
    console.log(`Hola ${alumno} bienvenido al curso`);
    
}
